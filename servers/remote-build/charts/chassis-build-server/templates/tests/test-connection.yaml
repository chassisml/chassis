apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chassis-build-server.fullname" . }}-test-connection"
  labels:
    {{- include "chassis-build-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chassis-build-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
