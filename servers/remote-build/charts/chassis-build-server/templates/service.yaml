apiVersion: v1
kind: Service
metadata:
  name: {{ include "chassis-build-server.fullname" . }}
  labels:
    {{- include "chassis-build-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chassis-build-server.selectorLabels" . | nindent 4 }}
