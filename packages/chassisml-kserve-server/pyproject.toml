[project]
name = "chassisml-kserve-server"
version = "2.0.0"
dependencies = [
]

#[tool.setuptools.packages]
#find = {}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.pyi"]